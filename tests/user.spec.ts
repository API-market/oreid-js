import { getOreId, generateToken } from './utils'

test('Try to retrieve user data without being authenticated', async () => {
  expect(1).toEqual(1)
})

test('Retrieve user data', async () => {
  expect(1).toEqual(1)
})

test('When updating user data, should call observer with the new data', async () => {
  expect(1).toEqual(1)
})

test("Get user's chainAccounts", async () => {
  expect(1).toEqual(1)
})

test('Send passwordless code - email', async () => {
  expect(1).toEqual(1)
})

test('Send passwordless code - telefone', async () => {
  expect(1).toEqual(1)
})

test('Check passwordless code', async () => {
  expect(1).toEqual(1)
})

test('Update chain permissions"', async () => {
  expect(1).toEqual(1)
})
